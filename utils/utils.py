import numpy as np


def generate_problem(n_jobs, n_machines, low, high):
    """
    A very good docstring
    """
    # TODO : make a working function out of this
    affectations = np.zeros((n_jobs, n_machines))
    durations = np.zeros((n_jobs, n_machines))
    return affectations, durations
